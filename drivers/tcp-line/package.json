{
  "name": "@sim-corp/driver-tcp-line",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build:native": "node ./scripts/build-native.js",
    "test": "pnpm run build:native && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sim-corp/driver-core": "workspace:*",
    "@sim-corp/schemas": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "vitest": "^2.1.3"
  }
}
