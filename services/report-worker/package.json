{
  "name": "@sim-corp/report-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm -C .. --filter @sim-corp/schemas run build && pnpm -C .. --filter @sim-corp/agent-runtime run build && pnpm -C .. --filter @sim-corp/roast-report-agent run build",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sim-corp/agent-runtime": "workspace:*",
    "@sim-corp/roast-report-agent": "workspace:*",
    "@sim-corp/schemas": "workspace:*",
    "fastify": "^4.28.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^2.1.3",
    "tsx": "^4.19.2",
    "vitest": "^2.1.3"
  }
}
